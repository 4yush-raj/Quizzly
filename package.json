{
  "name": "quizzly-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend && prisma generate --schema=backend/prisma/schema.prisma",
    "postinstall": "chmod +x ./node_modules/.bin/* || true"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.112.3",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.10.2"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.13.0"
  }
}
